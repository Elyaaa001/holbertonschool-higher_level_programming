#!/usr/bin/python3
argv = []
len(argv) == x
if x == 0:
    print("{} arguments.".format(x))
    if x > 0 :
        print("{} arguments:".format(x))
         for i in range(1, len(argv)):
            print(f"{i}: {arguments[i]}")`
